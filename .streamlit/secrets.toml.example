# Образец для Streamlit Cloud (App settings -> Secrets). Реальные значения сюда НЕ писать —
# файл попадает в репозиторий. Настоящие секреты вставляются только в настройках приложения.
#
# ВАЖНО: обычные ключи (DATABASE_URL, ADMIN_EMAILS) должны стоять ВЫШЕ любой секции [..],
# иначе TOML вложит их внутрь секции и приложение их не найдёт.

DATABASE_URL = "postgresql://..."
ADMIN_EMAILS = "admin@example.com"   # через запятую; эти люди — админы всегда

# Необязательно. Без TEAM_PASSWORD управление (время сбора, пары ASIN) открыто всем, у кого есть ссылка на сайт.
# С ним — только после ввода пароля (от 8 символов). Если строка окажется ниже секции [..] или пароль короче 8
# символов, управление закрывается, а не открывается по ошибке.
# TEAM_PASSWORD = "длинный пароль"
# MAX_ACTIVE_PAIRS = 1000   # предел активных пар: каждая пара тратит кредиты сбора

# Вход через Google. Без этой секции дашборд работает как раньше: открытый просмотр, без входа.
[auth]
redirect_uri = "https://<адрес-приложения>.streamlit.app/oauth2callback"
cookie_secret = "<любая длинная случайная строка, 32+ символов>"
client_id = "<из Google Cloud Console>"
client_secret = "<из Google Cloud Console>"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"

[gcp_service_account]
type = "service_account"
project_id = "your-project-id"
private_key_id = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
private_key = "-----BEGIN PRIVATE KEY-----\nMIIEvQ...\n-----END PRIVATE KEY-----\n"
client_email = "your-service-account@your-project-id.iam.gserviceaccount.com"
client_id = "123456789012345678901"
auth_uri = "https://accounts.google.com/o/oauth2/auth"
token_uri = "https://oauth2.googleapis.com/token"
auth_provider_x509_cert_url = "https://www.googleapis.com/oauth2/v1/certs"
client_x509_cert_url = "https://www.googleapis.com/robot/v1/metadata/x509/your-service-account%40your-project-id.iam.gserviceaccount.com"
universe_domain = "googleapis.com"

# Необязательно: укажите, если ваша таблица называется иначе.
[dashboard]
spreadsheet_name = "BSR_Competitors_Tracker"
